# The Raspberry Pi 4 running 64-bit Raspberry Pi OS. Cross-compiled from the
# development machine; see scripts/build.sh and scripts/deploy.sh.
[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"

[alias]
xtask = "run --package xtask --"
