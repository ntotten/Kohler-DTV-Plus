{
  "name": "parts-viewer",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive 3D viewer and STL exporter for appliance/fixture service parts. Deliberately independent of the DTV+ hardware app.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "vite-node scripts/verify-exports.ts",
    "check": "npm run typecheck && npm run test && npm run verify && npm run build"
  },
  "dependencies": {
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/three": "^0.184.0",
    "typescript": "~5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.9"
  }
}
